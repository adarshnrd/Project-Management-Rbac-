export enum UserRoleEnumType {
  Admin = 'Admin', // Have access to all resources
  Viewer = 'Viewer', // Viewer permission for viewing the developer work and accomplished works.
  ServiceUser = 'ServiceUser', // Normal permission for updating the TimeSheets.
  Editor = 'Editor', // Have access to design the payload or sections.for future purpose.
}
